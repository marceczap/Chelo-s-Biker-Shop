<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/register_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">

    <!-- Bottom Left Large Helmet Watermark Outline matching Figma -->
    <ImageView
        android:id="@+id/iv_register_watermark"
        style="@style/MarcaAguaCascoEsquina" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingHorizontal="28dp"
            android:paddingTop="20dp"
            android:paddingBottom="36dp">

            <!-- Back Arrow Button -->
            <ImageView
                android:id="@+id/btn_back_to_login"
                style="@style/BotonIconoNav"
                android:contentDescription="Volver"
                android:src="@drawable/ic_arrow_back"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <!-- Title Registro -->
            <TextView
                android:id="@+id/tv_register_title"
                style="@style/TextoTituloGrande"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Registro"
                app:layout_constraintBottom_toBottomOf="@id/btn_back_to_login"
                app:layout_constraintEnd_toStartOf="@id/ll_top_logo_mini"
                app:layout_constraintStart_toEndOf="@id/btn_back_to_login"
                app:layout_constraintTop_toTopOf="@id/btn_back_to_login" />

            <!-- Top Right Helmet Mini Logo -->
            <LinearLayout
                android:id="@+id/ll_top_logo_mini"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:orientation="vertical"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent">

                <ImageView
                    android:layout_width="38dp"
                    android:layout_height="38dp"
                    android:scaleType="fitCenter"
                    android:src="@drawable/ic_helmet_logo"
                    android:contentDescription="Logo" />

                <TextView
                    style="@style/TextoSecundario"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:fontFamily="serif"
                    android:text="Est. 2026"
                    android:textColor="@color/biker_black"
                    android:textSize="10sp"
                    android:textStyle="bold" />
            </LinearLayout>

            <!-- Subtitle Ingresa tu correo y contraseña -->
            <TextView
                android:id="@+id/tv_register_subtitle"
                style="@style/TextoSubtituloVintage"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="56dp"
                android:text="Ingresa tu correo y contraseña"
                android:textSize="15sp"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/tv_register_title" />

            <!-- Input Correo -->
            <EditText
                android:id="@+id/et_reg_email"
                style="@style/CampoTextoPill"
                android:layout_width="0dp"
                android:layout_height="46dp"
                android:layout_marginTop="16dp"
                android:hint="correo"
                android:inputType="textEmailAddress"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/tv_register_subtitle" />

            <!-- Input Contraseña -->
            <EditText
                android:id="@+id/et_reg_password"
                style="@style/CampoTextoPill"
                android:layout_width="0dp"
                android:layout_height="46dp"
                android:layout_marginTop="14dp"
                android:hint="contraseña"
                android:inputType="textPassword"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/et_reg_email" />

            <!-- Button ACCEDER -->
            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/btn_reg_acceder"
                style="@style/BotonPrimario"
                android:layout_width="160dp"
                android:layout_height="44dp"
                android:layout_marginTop="24dp"
                android:backgroundTint="@null"
                android:text="ACCEDER"
                android:textColor="@color/biker_black"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/et_reg_password" />

            <!-- Text o Accede desde una cuenta existente -->
            <TextView
                android:id="@+id/tv_social_auth_label"
                style="@style/TextoSubtituloVintage"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="48dp"
                android:text="o Accede desde una cuenta existente"
                android:textSize="14sp"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/btn_reg_acceder" />

            <!-- Social Login 3 Options (Google, Apple, Facebook) -->
            <LinearLayout
                android:id="@+id/ll_social_login_group"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="26dp"
                android:gravity="center_horizontal"
                android:orientation="vertical"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/tv_social_auth_label">

                <!-- 1. Google Button -->
                <ImageView
                    android:id="@+id/btn_auth_google"
                    android:layout_width="52dp"
                    android:layout_height="52dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:clickable="true"
                    android:contentDescription="Acceder con Google"
                    android:focusable="true"
                    android:src="@drawable/ic_google_logo" />

                <!-- 2. Apple Button -->
                <ImageView
                    android:id="@+id/btn_auth_apple"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:layout_marginTop="20dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:clickable="true"
                    android:contentDescription="Acceder con Apple"
                    android:focusable="true"
                    android:src="@drawable/ic_apple_logo" />

                <!-- 3. Facebook Button -->
                <ImageView
                    android:id="@+id/btn_auth_facebook"
                    android:layout_width="50dp"
                    android:layout_height="50dp"
                    android:layout_marginTop="20dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:clickable="true"
                    android:contentDescription="Acceder con Facebook"
                    android:focusable="true"
                    android:src="@drawable/ic_social_fb" />
            </LinearLayout>

        </androidx.constraintlayout.widget.ConstraintLayout>
    </ScrollView>
</FrameLayout>
